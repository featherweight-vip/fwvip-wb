
package:
  name: org.featherweight-vip.fwvip-wb
  with:
    sim:
      type: str 
      value: "vlt"


  imports:
  - "${{ rootdir }}/packages/fwprotocol-defs/flow.yaml"

  tasks: []

  fragments:
  - src/vip/vip.yaml
  - src/verif/verif.yaml
